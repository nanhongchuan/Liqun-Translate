---
description: TypeScript / React 与 Python 的代码与命名规范
alwaysApply: true
---

# 代码与命名规范

## 通用

- 优先 **小改动、单用途提交**；不引入与任务无关的大范围重构。
- 配置、厂商与 manifest **配置驱动**（常量集中或自描述结构），减少散落魔法字符串。
- 用户可见中文文案与产品名可本地化；代码标识符、提交说明可用 **英文** 或与团队约定一致。

## TypeScript / React

- 使用 **`.tsx`** 表示带 JSX 的组件；**`PascalCase`** 组件与类型；**`camelCase`** 函数、变量、props。
- 文件名：`LandingPage.tsx` 等 **PascalCase** 页面/大组件；小工具可 `kebab-case` 或 `camelCase` 与现有文件一致（当前仓库为 **PascalCase** 页面组件）。
- `use` 前缀自定义 Hook；按功能拆分组件，**避免** 巨型单文件。
- 严格模式依赖 `tsconfig`；避免 `any`；异步错误需有用户可感知或日志路径。

## Python（后端，落地后统一）

- **PEP 8**；模块与包 `snake_case`；类 **`PascalCase`**；**类型注解** 在新代码中采用。
- FastAPI 路由与依赖注入分离业务逻辑到 **服务层** 函数或类，避免路由函数过长。

## 接口与数据

- 与后端交换的 DTO/JSON 字段名前后端 **一致**；WebSocket/REST 消息版本或字段变更时留注释或小幅兼容期。

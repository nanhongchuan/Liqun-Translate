---
description: Tailwind、Radix 与产品视觉层（侧栏/落地页/同传）规范
globs: "**/*.{tsx,css,jsx}"
alwaysApply: false
---

# 样式与 UI 规范

## 技术栈

- 布局与动效用 **Tailwind**；可访问性表单/对话框/选项卡用 **Radix**；图标用 **lucide-react**（与现有一致）。

## 布局与状态（PRD §4.1）

- **侧栏**：深色背景、当前项高亮；含「同传/首页」「设置」等；主导航固定左侧。
- **主内容区**：**落地页**（默认，浅 **蓝—紫** 线性渐变、大圆角、居中 **主 CTA**）与 **同传进行中**（玻璃拟态 `backdrop-blur` 气泡：上层原文、下层译文；底部**悬浮**控制条：麦克/计时/停止）两态可切换；顶栏**语言方向**在两种主视图间**状态一致**。

## 设计令牌（在 `tailwind.config` 与 class 中复用）

- 主区：浅蓝—淡紫渐变；卡片 **白/半透 + blur**；侧栏与主区 **明度对比** 区分「导航 / 任务」。
- 已有扩展：`boxShadow.glass` 等应继续用于玻璃卡片；新增色值在 **theme.extend** 或 CSS 变量中集中，避免同义灰字散落。

## 实践

- 优先 **utility-first**；重复三次以上的模式可抽 **`<div className={cn(...)}>`** 或小组件。
- 首屏与列表注意 **长列表/滚动性能**；避免在热路径上无意义的重渲染。

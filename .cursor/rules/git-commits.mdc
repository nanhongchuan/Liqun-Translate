---
description: Git 提交信息格式与内容约定
alwaysApply: true
---

# 提交规范

## 语言与格式

- 提交说明用 **完整句子** 或等价的祈使/陈述句，避免无意义占位（如 "fix" 单独一行）。
- 推荐前缀（与 Conventional Commits 兼容、便于阅读）：
  - `feat:` 新功能
  - `fix:` 缺陷修复
  - `refactor:` 重构（行为不变）
  - `style:` 仅格式/样式无逻辑
  - `chore:` 构建、依赖、脚本
  - `docs:` 文档
  - `test:` 测试

**示例**：

```
feat: add live session page layout and bottom control bar
fix: avoid leaking API key in client-side storage
```

## 内容要求

- 一条提交聚焦 **一个主题**；大范围变更时拆成多条更易审查。
- 与 PRD/安全相关变更在正文或脚注中 **简短** 说明影响（如「Key 仅后端保存」），不必冗长。
